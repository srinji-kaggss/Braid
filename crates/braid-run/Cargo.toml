[package]
name = "braid-run"
version = "0.1.0"
edition = "2021"
description = "Deterministic DAG interpreter and runtime execution engine for Braid capsules"
license = "MIT OR Apache-2.0"

[dependencies]
braid-capability = { path = "../braid-capability" }
braid-ir = { path = "../braid-ir" }
lgwks_std = { path = "../lgwks-std" }

[dev-dependencies]
braid-verify = { path = "../braid-verify" }
