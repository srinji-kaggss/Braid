[package]
name = "braid-run"
version = "0.1.0"
edition = "2024"
description = "Deterministic DAG interpreter and runtime execution engine for Braid capsules"
license = "MIT OR Apache-2.0"

[dependencies]
braid-capability = { workspace = true }
braid-flow-ir = { workspace = true }
braid-flow-plan = { workspace = true }
braid-ir = { workspace = true }
braid-verify = { workspace = true }
lgwks_std = { workspace = true }
