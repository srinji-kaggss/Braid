[package]
name = "lgwks_bot"
description = "Capability-gated automation bots built on four verbs: Observe, Evaluate, Execute, Query."
version = "0.1.1"
edition = "2024"
rust-version = "1.98"
license = "BSD-3-Clause"
repository = "https://github.com/srinji-kaggss/Braid"
homepage = "https://github.com/srinji-kaggss/Braid/tree/main/crates/lgwks-bot"
readme = "README.md"
categories = ["development-tools", "config"]
keywords = ["automation", "bot", "capabilities", "observe", "execute"]

[dependencies]
lgwks_std = { workspace = true, features = ["json"] }
serde = { workspace = true }

[dev-dependencies]
